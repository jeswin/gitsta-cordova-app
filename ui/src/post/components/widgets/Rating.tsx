import * as React from "react";
import { WidgetProps } from "../Post";
import { UICell, RatingCell } from "../../../types";

export default function Rating(props: WidgetProps<RatingCell>) {
  return <div>Audio</div>;
}
