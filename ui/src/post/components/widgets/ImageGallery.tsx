import * as React from "react";
import { WidgetProps } from "../Post";
import { UICell, ImageGalleryCell } from "../../../types";

export default function ImageGallery(props: WidgetProps<ImageGalleryCell>) {
  return <div>Image Gallery</div>;
}
