import * as React from "react";
import { UICell, AudioCell } from "../../../types";
import { WidgetProps } from "../Post";

export default function Audio(props: WidgetProps<AudioCell>) {
  return <div>Audio</div>;
}
