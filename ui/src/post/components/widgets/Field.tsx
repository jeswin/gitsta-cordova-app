import * as React from "react";
import { UICell, FieldCell } from "../../../types";
import { WidgetProps } from "../Post";

export default function Field(props: WidgetProps<FieldCell>) {
  return <div>Audio</div>;
}
