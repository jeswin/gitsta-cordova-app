import * as React from "react";
import { UICell, HashtagsCell } from "../../../types";
import { WidgetProps } from "../Post";

export default function Hashtags(props: WidgetProps<HashtagsCell>) {
  return <div>Audio</div>;
}
