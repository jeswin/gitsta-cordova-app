import * as React from "react";
import { WidgetProps } from "../Post";
import { UICell, LocationCell } from "../../../types";

export default function Location(props: WidgetProps<LocationCell>) {
  return <div>Location</div>;
}
