import * as React from "react";
import { WidgetProps } from "../Post";
import { UICell, VideoCell } from "../../../types";

export default function Video(props: WidgetProps<VideoCell>) {
  return <div>Video</div>;
}
