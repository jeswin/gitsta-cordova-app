import * as React from "react";
import { WidgetProps } from "../Post";
import { UICell, TableCell } from "../../../types";

export default function Table(props: WidgetProps<TableCell>) {
  return <div>Audio</div>;
}
