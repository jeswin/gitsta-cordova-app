import * as React from "react";
import { WidgetProps } from "../Post";
import { UICell, MarkdownCell } from "../../../types";

export default function Markdown(props: WidgetProps<MarkdownCell>) {
  return <div>Markdown</div>;
}
